function powersOfTwo(n){
    let result = []
    for (let i = 0; i <= n; i++) {
       result.push(2 ** i)
    }
    return result
  }

  //
  function powersOfTwo(n) {
   let empty =[]
   for(let i =0; i < empty.length; i++) {

   }
  }
// closer but the push was wrong
 function powersOfTwo(n) {
   let empty = []
   for(let i = 0; i < n; i++) {
      return (2 ** n) 
      push 
   }
 }
// wrong number
 function powersOfTwo(n) {
   let result = []
   for(let i = 0; i < n; i++) {
      result.push(2 ** n)
   }
   return result
}

